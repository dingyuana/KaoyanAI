| | |
|---|---|
|定义|- 允许两端都可以进行入队和出队操作的队列<br>- 将队列的两端分别称为前端和后端|
|特点|- 其元素的逻辑结构仍是线性结构|
|分类|<font color="#f79646">输出受限的双端队列</font><br>- 允许在一段进行插入和删除，但在另一端只允许插入的双端队列<br>- 做题的时候输出固定, 看输入<br><font color="#f79646">输入受限的双端队列</font><br>- 允许在一段进行插入和删除，但在另一端只允许删除的双端队列<br>- 做题的时候输入固定, 看输出|
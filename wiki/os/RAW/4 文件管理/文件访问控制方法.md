- <font color="#c0504d">访问控制的目的</font>：用于控制用户对文件的访问方式
- <font color="#c0504d">访问控制的对象</font>：读；写；执行；添加；删除；列表清单
- 访问控制机制必须由系统实现

| |<font color="#c0504d">方法一</font>|<font color="#c0504d">方法二</font>|
|---|---|---|
|<font color="#c0504d">定义</font>|- 为每个文件和目录增加一个访问控制列表ACL<br>- 该表规定每个用户名及其所允许的空间管理|- 采用精简的访问列表<br>- 该列表采用<font color="#4f81bd">拥有者</font>、<font color="#4f81bd">组</font>和<font color="#4f81bd">其他</font>三种用户类型|
|<font color="#c0504d">优点</font>|- 可以使用复杂的访问方法|- 只需要三个域即可列出访问表中这三类用户的访问权限|
|<font color="#c0504d">缺点</font>|- 长度无法预计并且可能导致复杂的空间管理||
| | |
|---|---|
|1. <font color="#c0504d">顺序文件</font>|- 串结构：只能按顺序查找，费时<br>- 顺序结构：可采用折半查找，检索效率高<br>- <font color="#4f81bd">顺序查找平均次数$\frac{N}{2}$</font>；<font color="#4f81bd">索引顺序查找平均次数$N^{\frac{1}{2}}$</font>|
|2. <font color="#c0504d">索引文件</font>|- 提高了存取速度，但索引表增加了存储空间|
|3. <font color="#c0504d">索引顺序文件</font>|- 同一组中的关键字可以无序，但组与组之间的关键字必须有序<br>- 先通过索引表查找所在的组，然后在该组中使用顺序查找|
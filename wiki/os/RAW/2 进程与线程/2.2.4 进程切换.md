| | |
|---|---|
|<font color="#8064a2">上下文</font>|上下文是指某一时刻CPU寄存器和PC的内容；如寄存器值，用户和系统内核栈内容|
|<font color="#8064a2">进程上下文切换</font>|上下文切换是指一个进程切换到另一个进程的过程|
|<font color="#8064a2">上下文切换的实质</font>|上下文切换实质是处理机从一个进程的运行转到另一个进程上运行，这个过程中，进程的运行环境产生了实质性的变化|
|<font color="#8064a2">上下文切换的消耗</font>|有些处理器提供多个寄存器组，上下文切换只需要简单改变当前寄存器组的指针，不需要用到磁盘和主存|
|<font color="#8064a2">上下文切换的场景</font>|1. 某个进程时间片耗尽时<br>2. 进程在系统资源不足时，要等到资源满足后才可以运行<br>3. 进程通过sleep将自己主动挂起<br>4. 有优先级更高的进程运行时<br>5. 发送硬件中断时|
|<font color="#8064a2">上下文切换的流程</font>|1. 挂起一个进程，保存CPU上下文，包括PC和其他寄存器<br>2. 更新PCB信息<br>3. 把进程的PCB移到相应的队列（如就绪队列，阻塞队列）<br>4. 加载另一个进程执行，更新其PCB<br>5. 跳转到新进程PCB中的PC所指向的位置执行<br>6. 恢复处理机上下文|
|<font color="#8064a2">示例图</font>|![[进程上下文切换示例图.jpg]]|
|<font color="#8064a2">调度和切换的区别</font>|![[调度和切换的区别]]|

## 
---
[[2.1.6 线程常考的知识点]]
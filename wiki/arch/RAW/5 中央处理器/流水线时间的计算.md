1. 设一条指令有3条流水段，每段平均时间为t，度为1
	单流水线处理机执行12条指令的时间 = $\color{red}{3t + (12-1) t}$ = $\text{一条指令所需时间} + (\text{指令条数}-1) \times \text{时间最长的指令的一段}$
2. 设一条指令有3条流水段，每段平均时间为t，<font color="#c0504d">度为4</font>
	单流水线处理机执行20条指令的时间 = $\color{red}{3t + \frac{20-4}{4}t}$ 📢度为4说明是超标量流水线处理机，一次可以发送4条指令
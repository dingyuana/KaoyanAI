- 以<font color="#c0504d">加法运算</font>为例

| | |
|---|---|
|Ad(IR) → Bus → MAR|MDRout和MARin有效|
|1 → R|CU发出读命令|
|MEN → 数据线 → MDR|操作数从存储器 → 数据线 → MDR|
|MDR → Bus → Y|MDRout和Yin有效，有效数 → Y|
|(ACC)+(Y) → Z|ACCout和ALUin有效，CU向ALU发加命令，结果 → Z|
|Z → ACC|Zout和ACCin有效，结果 → ACC|
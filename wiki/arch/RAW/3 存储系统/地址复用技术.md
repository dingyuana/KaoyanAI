- 由于DRAM芯片容量大，地址位数多，为了减少地址引脚线，采用<font color="#c0504d">地址复用技术</font>
- DRAM因为分两次发送，长度相同，因此地址线可以复用，线数减少了一半
- <font color="#c0504d">引脚数</font> = 地址线减半 + 数据线不变 + 行通选(1) + 列通选(1) + 读写控制线(2)
- 片选线用行通选线替代↑
- <font color="#c0504d">DRAM采用地址复用技术</font>【计算引脚时不要忘记了】，<font color="#c0504d">SRAM不采用</font>
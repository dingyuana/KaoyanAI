- 指令长度固定，指令格式种类少，寻址方式种类少，指令条数少
- <font color="#c0504d">只有Load/Store指令访存</font>，其他指令的操作在寄存器中进行
- CPU中通用寄存器的数量相当多
- 以硬布线控制为主，不用或少用微程序控制
- 有利于实现指令流水线的特点
	- 指令格式规整且长度一致
	- 指令和数据按边界对齐存放
	- 只有Load/Store指令能访问存储器
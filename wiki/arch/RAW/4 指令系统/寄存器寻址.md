【<font color="#9bbb59">定义</font>】
- 和直接寻址原理一样，只是把访问主存改为访问寄存器

【<font color="#9bbb59">特点</font>】
- 寄存器寻址主要执行<font color="#c0504d">取指令访存</font>1次
- 由于访问的是寄存器因此<font color="#c0504d">不需要执行指令访存</font>
- 访问寄存器会比访问主存快得多
- CPU中寄存器不是很多，用很短的编码就可以指定寄存器，能有效地<font color="#c0504d">缩短地址段的位数</font>
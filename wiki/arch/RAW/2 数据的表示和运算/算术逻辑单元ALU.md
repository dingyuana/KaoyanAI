- <font color="#c0504d">ALU</font>的核心是带标志加法器
- ALU是功能强大的组合逻辑电路，能进行算术运算，逻辑运算，移位操作
- <font color="#c0504d">ALUop</font>是操作控制端，决定ALU所执行的处理操作
- ALUop的位数决定了操作的种类，位数为3时，有 $2^3=8$ 种操作
- 在ALUop的控制下，由一个多路选择器MUX选择输出某种操作结果
- <font color="#c0504d">MUX</font>是多路选择开关，它从多个输入信号中选择一个送到输出端